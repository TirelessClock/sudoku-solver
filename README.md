## sudoku solver

takes a sudoku puzzle screenshot input and solves it.

add screenshot as `image.png` and run `sudoku_solver.py`.

note: the digit classifier is bad at decoding 7 for some reason. there's a way to manually update the puzzle in that case. 
