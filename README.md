## sudoku solver

takes a sudoku puzzle screenshot input and solves it.

just replace `image.png` with the screenshot and run `sudoku_solver.py`.

note: the digit classifier is bad at decoding 7 for some reason.
